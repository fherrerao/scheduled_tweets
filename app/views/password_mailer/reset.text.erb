Hi <%= params[:user].email %>

Someone requested a reset of your password.

If this was you, clicl the link below to reset the password. The link will expire automatically in 15 minutes.
<%= password_reset_edit_url(token: @token)%>
